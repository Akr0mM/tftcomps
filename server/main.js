import { Meteor } from 'meteor/meteor';
import '../imports/api/users/methods';
import '../imports/api/compositions/methods';
import '../imports/api/compositions/publish';

Meteor.startup(() => {
  // code to run on server at startup
});
